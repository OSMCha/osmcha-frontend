// @flow
import React from 'react';
import { Map, List } from 'immutable';
import { Reasons } from '../reasons';
import { importChangesetMap } from '../../utils/cmap';

export function TagChanges({
  properties,
  changesetId
}: {
  properties: Map<string, *>,
  changesetId: number
}) {
  var tag_changes = <div>test</div>;
  return tag_changes;
}
